<section class="player-profile">
    <div class="player-profile__card">
        <img class="player-profile__avatar" src="<?= $player->getPortrait() ?>" alt="<?= $player->getNickname() ?>">
        <div class="player-profile__info">
            <h1 class="player-profile__nickname"><?= $player->getNickname() ?></h1>
            <p class="player-profile__team"><?= $player->getTeamName() ?></p>
            <p class="player-profile__bio"><?= $player->getBio() ?></p>
        </div>
    </div>

    <div class="player-teammates">
        <h2>Ses coéquipiers</h2>
        <div class="player-teammates__grid">
            <?php foreach($coequipiers as $coequipier) : ?>
                <?php if($coequipier->getId() !== $player->getId()) : ?>
                    <div class="player-card">
                        <img src="<?= $coequipier->getPortrait() ?>" alt="<?= $coequipier->getNickname() ?>">
                        <p class="player-card__nickname"><?= $coequipier->getNickname() ?></p>
                    </div>
                <?php endif; ?>
            <?php endforeach; ?>
        </div>
    </div>
</section>
