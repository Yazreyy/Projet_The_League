<section class="players-page">
    <h1 class="players-page__title">Les Players</h1>
    <div class="players-grid">
        <?php foreach($players as $player) : ?>
            <div class="player-card">
                <img src="<?= $player->getPortrait() ?>" alt="<?= $player->getNickname() ?>">
                <p class="player-card__nickname"><?= $player->getNickname() ?></p>
                <p class="player-card__team"><?= $player->getTeamName() ?></p>
                <a class="player-card__link" href="index.php?route=player&id=<?= $player->getId() ?>">Découvrir</a>
            </div>
        <?php endforeach; ?>
    </div>
</section>
